const {
  runAccessibilityChecks,
  normalizeOptions,
  normalizeFinding,
} = require("./runAccessibilityChecks");

module.exports = {
  runAccessibilityChecks,
  normalizeOptions,
  normalizeFinding,
};